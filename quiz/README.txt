This is a Front-End Appliaction/Game I created using React.
In order to run this application, cd into the "quiz" folder and run "npm install" to install all needed dependencies.
Once every dependency is installed run "npm start" and the application should be running on "localhost:3000".
